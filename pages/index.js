import Layout from '../components/Layout';

export default function Home() {
  // i pass the title as a prop in the layout to get the
  // title on the tabbar
  return <Layout title="Home Page">Home</Layout>;
}
